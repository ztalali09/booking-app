{
  "version": 2,
  "env": {
    "NODE_ENV": "production"
  },
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "buildCommand": "npx prisma generate && npm run build",
  "installCommand": "npm install"
}
